[project]
name = "string-diagram-mcp"
version = "2.0.0"
description = "Category-theoretic string diagram visualization — any category, any domain"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
